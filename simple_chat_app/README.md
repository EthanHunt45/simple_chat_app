simple_chat_app

A mini mobile programming project for who is new to Dart language and Flutter framework. This project
also use Firebase so before you run it first you must fill the firebase_options and firebase.json.

Getting Started

First for the dependencies, you need to run
```bash
flutter pub get
```

Then after installing all dependencies, you can run it on your emulator or connected device
```bash
flutter run
```